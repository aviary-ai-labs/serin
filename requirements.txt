fastapi==0.136.1
uvicorn==0.30.0
python-dotenv==1.2.2
pydantic>=2.12.4,<3
httpx==0.27.0
python-multipart==0.0.27
# Pinned to a major line: 13.0 changed the client constructor (auth modes
# replaced bare consumer_key/client_id) and an unpinned rebuild picked it
# up silently, turning every SnapTrade call into a TypeError.
snaptrade-python-sdk>=13.0.0,<14.0.0
cryptography>=44
# Only used when SERIN_DATABASE_URL points at Postgres (shared deployments).
# Self-hosters run SQLite and never import it — but it has to be installed in
# the image, because dbdriver imports it lazily and would otherwise fail on the
# first request after deploy rather than at build time. The `pool` extra is not
# optional on that path: unpooled, each query pays a fresh TLS handshake and
# the handshakes alone ran to gigabytes of egress a month.
psycopg[binary,pool]>=3.2,<4
# Renders SECURITY.md / docs/*.md as the site's /security, /privacy, /terms
# and /deploy pages, so policy links don't hand visitors off to GitHub.
markdown>=3.6
# Smart Import rasterizes PDF pages to images locally (with Pillow encoding
# the PNGs), so any vision-capable AI provider can read statements — and only
# pixels ever leave the machine.
pypdfium2>=4.30
pillow>=10
